# 二开特性说明 (FEATURE.md)

## 从节点支持 ApiToken 自动登录
- **价值与场景**：在多节点部署场景下，中央主节点管理多个从节点。之前管理员在主面板 `/panel/nodes` 页面点击从节点地址时，需要在新打开的从节点网页中重新输入账号密码登录，运维体验繁琐。
- **功能描述**：
  1. 主节点提供 `/panel/api/nodes/loginUrl/:id` 接口，管理员在节点列表点击从节点地址时，动态获取携带从节点 API Token 的登录链接并在新标签页中打开。
  2. 从节点在根入口页面（`GET /`）自动识别 URL 中的 `apiToken` 参数并校验权限（需为启用的 `admin` 权限 Token），校验通过后自动创建 Session 并跳转至 `/panel/` 后台界面，无需手动输入用户名和密码。

## Xray 版本选择与版本锁定
- **价值与场景**：在 Xray-core 上游版本快速迭代或新版协议出现客户端不兼容（如 Reality 协议由于强制协商算法导致部分客户端无法连接）的场景下，管理员需要将 Xray-core 固定在特定稳定版本或指定任意旧版本；同时希望升级 3X-UI 面板自身时，不会强行覆盖升级 Xray-core 核心。
- **功能描述**：
  1. **版本切换界面重构**：将原版本单选列表重构为下拉选择框，支持搜索历史版本与手动输入任意合法版本号（如 `v26.7.28`）。
  2. **版本锁定保护**：支持勾选「是否锁定版本」。锁定后系统生成标记并在配置中持久化。
  3. **升级隔离**：在面板自更新（`update.sh`）或安装脚本更新（`install.sh`）时检测锁定标记，若已锁定则自动备份并还原当前 Xray 核心二进制文件，避免被新版安装包自带的核心覆盖。

## 从节点面板及 Xray 核心远程重启
- **价值与场景**：在多节点分布式集群管理中，从节点配置调整或偶发运行异常时，管理员需要能够直接在中央控制台对远程从节点进行面板及 Xray 服务的快速重启，无需逐台 SSH 登录服务器执行指令。
- **功能描述**：
  1. 主节点提供 `/panel/api/nodes/restart/:id` 接口，通过受信通道（mTLS / Node Token）下发 RPC 调用从节点的 `/panel/api/setting/restartPanel`。
  2. 从节点安全执行面板热重启并带动 Xray 进程重启；在主面板节点列表（桌面端操作列及移动端操作菜单）提供「重启」按钮及二次确认交互。

## 主从节点代理出站双向同步与全网扩散
- **价值与场景**：在多节点集群部署中，管理员配置中转线路或代理出站（Outbound）时，原先必须逐台登录各个从节点的控制台去配置，无法在主控台集中管控；同时从节点上原有的出站也无法回传同步，导致多节点间中转出站配置繁琐且不一致。
- **功能描述**：
  1. **代理出站去重与识别**：支持识别标准代理协议出站（`vmess`, `vless`, `trojan`, `shadowsocks`, `hysteria`, `wireguard`, `socks`, `http`），基于协议+目标地址+端口（`protocol://host:port`）自动进行全局唯一去重，保留本地内置基础规则出站（如 `freedom`, `blackhole`, `dns`）。
  2. **从节点出站汇聚**：从节点配置的代理出站通过后台心跳同步机制自动汇聚上报到主节点，主节点无感采纳并合并进全局 Xray 模板。
  3. **主控优先全网扩散与删除同步**：主控台新增、修改或删除代理出站后，自动标记所有从节点脏状态，在节点对齐（Reconcile）阶段全量下发覆盖至所有从节点；冲突时以主控配置优先，支持双向删除同步，确保集群所有节点出站配置完全一致。

## 前端迁移至 pnpm 包管理器与依赖拓扑治理
- **价值与场景**：原前端构建使用 npm，存在幽灵依赖隐患且依赖安装占用磁盘空间大、安装速度慢；在本地和 CI 环境中难以保证依赖拓扑严格隔离与确定性。
- **功能描述**：
  1. **包管理器迁移**：将前端（`frontend/`）包管理器完整切换为 `pnpm`（lockfile v9），生成并维护 `pnpm-lock.yaml`，移除旧的 `package-lock.json`。
  2. **依赖拓扑补全**：修复此前由于 npm 扁平化（hoisting）掩盖的幽灵依赖问题，在 `package.json` 中显式声明直接引用的 `@codemirror/*` 核心模块与 `@testing-library/user-event` 测试工具。
  3. **自动化构建与 CI 适配**：同步适配 `Makefile`、`Dockerfile` 以及 GitHub Actions 流水线（`ci.yml`、`release.yml`、`codeql.yml`、`docs-deploy.yml`），统一使用 `pnpm/action-setup@v6` 与 `pnpm` 相关指令。

## 跨节点路由规则配置与节点级入站标签隔离
- **价值与场景**：在多节点分布式部署场景下，路由规则原先只能登录各个从节点的控制台分别进行配置，无法在主控台统一管控；且配置路由时如果展示了非本节点的入站标签，极易引起配置混乱与误操作。
- **功能描述**：
  1. **节点路由统一管控**：在路由规则界面新增「选择节点」下拉选择框，支持选择本地面板（主节点）或任意从节点。选择对应从节点后，可直接在该节点上查看、新增、编辑、删除、拖拽排序与导入导出路由规则，点击保存后通过远程 RPC 自动推送到目标节点生效。
  2. **入站标签节点级作用域隔离**：在配置规则的入站标签选择器及规则表格中，根据当前选中的节点严格过滤入站列表，仅展示属于该节点的入站标签及备注，杜绝跨节点入站标签混入导致配置失效或混淆。
