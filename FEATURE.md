# 二开特性说明 (FEATURE.md)

## 从节点支持 ApiToken 自动登录
- **价值与场景**：在多节点部署场景下，中央主节点管理多个从节点。之前管理员在主面板 `/panel/nodes` 页面点击从节点地址时，需要在新打开的从节点网页中重新输入账号密码登录，运维体验繁琐。
- **功能描述**：
  1. 主节点提供 `/panel/api/nodes/loginUrl/:id` 接口，管理员在节点列表点击从节点地址时，动态获取携带从节点 API Token 的登录链接并在新标签页中打开。
  2. 从节点在根入口页面（`GET /`）自动识别 URL 中的 `apiToken` 参数并校验权限（需为启用的 `admin` 权限 Token），校验通过后自动创建 Session 并跳转至 `/panel/` 后台界面，无需手动输入用户名和密码。

## Xray 版本选择与版本锁定
- **价值与场景**：在 Xray-core 上游版本快速迭代或新版协议出现客户端不兼容（如 Reality 协议由于强制协商算法导致部分客户端无法连接）的场景下，管理员需要将 Xray-core 固定在特定稳定版本或指定任意旧版本；同时希望升级 3X-UI 面板自身时，不会强行覆盖升级 Xray-core 核心。
- **功能描述**：
  1. **版本切换界面重构**：将原版本单选列表重构为下拉选择框，支持搜索历史版本与手动输入任意合法版本号（如 `v26.7.28`）。
  2. **版本锁定保护**：支持勾选「是否锁定版本」。锁定后系统生成标记并在配置中持久化。
  3. **升级隔离**：在面板自更新（`update.sh`）或安装脚本更新（`install.sh`）时检测锁定标记，若已锁定则自动备份并还原当前 Xray 核心二进制文件，避免被新版安装包自带的核心覆盖。

## 从节点面板及 Xray 核心远程重启
- **价值与场景**：在多节点分布式集群管理中，从节点配置调整或偶发运行异常时，管理员需要能够直接在中央控制台对远程从节点进行面板及 Xray 服务的快速重启，无需逐台 SSH 登录服务器执行指令。
- **功能描述**：
  1. 主节点提供 `/panel/api/nodes/restart/:id` 接口，通过受信通道（mTLS / Node Token）下发 RPC 调用从节点的 `/panel/api/setting/restartPanel`。
  2. 从节点安全执行面板热重启并带动 Xray 进程重启；在主面板节点列表（桌面端操作列及移动端操作菜单）提供「重启」按钮及二次确认交互。

## 控制端权威代理出站同步与从节点对齐
- **价值与场景**：在多节点集群部署中，管理员配置中转线路或代理出站（Outbound）时，原先必须逐台登录各个从节点的控制台去配置，无法在主控台集中管控；同时多节点混合部署（部分二开版节点、部分官方版节点）时，由于缺少接口或单节点推送失败极易导致同步卡死。
- **功能描述**：
  1. **代理出站识别与模板重组**：支持识别标准代理协议出站（`vmess`, `vless`, `trojan`, `shadowsocks`, `hysteria`, `hysteria2`, `wireguard`, `socks`, `http`），同步时仅替换代理出站，严格保留各节点内置基础规则出站（如 `freedom`, `blackhole`, `dns`）。
  2. **控制端权威单向对齐**：以控制端节点为准，控制端配置的代理出站自动推送到从节点对齐。控制端有的从节点一定有，控制端删除的出站自动同步删除至从节点；从节点新增的出站不同步回控制端。
  3. **单节点故障隔离与官方节点兼容**：控制端推送各从节点在独立并发协程中执行，单个节点推送失败（超时或异常）互不阻塞；针对未实现出站同步接口的官方版/旧版从节点（返回 404），自动容忍并清除脏标记，避免节点陷入 config_dirty 重试循环。
  4. **特殊出站（WARP）隔离保护**：Cloudflare WARP（出站协议为 `wireguard`，标签为 `warp`、`warp-*` 或 `warp_*`）属于节点本地专用出站，具有独立的密钥和端点配置，不参与跨节点代理出站同步。主从同步时从节点本地 WARP 出站严格予以保留，主节点的 WARP 出站也不会同步下发，避免从节点重启或重对齐后 WARP 丢失。

## 前端迁移至 pnpm 包管理器与依赖拓扑治理
- **价值与场景**：原前端构建使用 npm，存在幽灵依赖隐患且依赖安装占用磁盘空间大、安装速度慢；在本地和 CI 环境中难以保证依赖拓扑严格隔离与确定性。
- **功能描述**：
  1. **包管理器迁移**：将前端（`frontend/`）包管理器完整切换为 `pnpm`（lockfile v9），生成并维护 `pnpm-lock.yaml`，移除旧的 `package-lock.json`。
  2. **依赖拓扑补全**：修复此前由于 npm 扁平化（hoisting）掩盖的幽灵依赖问题，在 `package.json` 中显式声明直接引用的 `@codemirror/*` 核心模块与 `@testing-library/user-event` 测试工具。
  3. **自动化构建与 CI 适配**：同步适配 `Makefile`、`Dockerfile` 以及 GitHub Actions 流水线（`ci.yml`、`release.yml`、`codeql.yml`、`docs-deploy.yml`），统一使用 `pnpm/action-setup@v6` 与 `pnpm` 相关指令。

## 节点列表操作收纳与批量删除/重启/更新
- **价值与场景**：多节点集群中，桌面端操作列原先并排展示探测、更新、重启、编辑、删除图标，列宽拥挤且易误点；批量操作仅支持更新，且勾选被限制为在线节点，无法对离线节点做批量删除。
- **功能描述**：
  1. **行内更多菜单**：桌面端操作列将「立即探测」「重启面板」「编辑」「删除」收纳到更多图标下拉中，在线节点仍保留「更新面板」快捷图标。移动端继续使用更多菜单（含更新）。
  2. **批量操作**：选中节点后工具栏提供批量更新、批量重启、批量删除；勾选不再限制为在线节点，离线节点也可纳入批量删除。重启/更新仍只对已启用且在线的节点生效。

## 跨节点路由规则配置与节点级入站标签隔离
- **价值与场景**：在多节点分布式部署场景下，路由规则原先只能登录各个从节点的控制台分别进行配置，无法在主控台统一管控；且配置路由时如果展示了非本节点的入站标签，极易引起配置混乱与误操作。
- **功能描述**：
  1. **节点路由统一管控**：在路由规则界面新增「选择节点」下拉选择框，支持选择本地面板（主节点）或任意从节点。选择对应从节点后，可直接在该节点上查看、新增、编辑、删除、拖拽排序与导入导出路由规则，点击保存后通过远程 RPC 自动推送到目标节点生效。
  2. **入站标签节点级作用域隔离**：在配置规则的入站标签选择器及规则表格中，根据当前选中的节点严格过滤入站列表，仅展示属于该节点的入站标签及备注，杜绝跨节点入站标签混入导致配置失效或混淆。控制端为去重给节点入站加的 `n{id}-` 前缀只存在于控制端；编辑/下发从节点路由时使用节点本地原生 tag，避免子节点 Xray 因 tag 对不上而路由失败。
  3. **出站标签节点级作用域隔离**：选择从节点后，规则编辑里的出站下拉仅列出该节点模板中实际存在的出站（含系统出站如 `direct`/`blocked`），不再展示控制端本机出站。`POST /panel/api/xray/` 在响应中新增 `outboundTags` 字段。

## 入站节点与多客户端内核级下行限速
- **价值与场景**：防止单个用户或单个入站节点突发流量跑满服务器出口带宽，避免高并发下导致服务降级或整机网络拥塞；满足多租户按需分配带宽、控制节点下行峰值速率的精细化流控运维需求。
- **功能描述**：
  1. **Linux 内核级 HTB 流量整形**：基于 Linux `tc`（Traffic Control）分层令牌桶（HTB）树状队列规则实现，限速逻辑下沉至操作系统内核网络栈，旁路零 CPU 额外损耗，完全兼容上游官方 Xray 核心任意版本切换。
  2. **双层层级限速模型**：
     - **入站整站限速（Inbound Downlink Limit）**：限制特定入站端口的所有下行总聚合带宽。
     - **单客户端隔离限速（Client Downlink Limit）**：在入站根限速下挂载独立子类（Class），各客户端独占限速令牌，互不抢占带宽。
  3. **在线客户端 IP 动态对齐**：紧密贴合 Xray 访问日志与在线 IP 探测机制，实时监听在线客户端 IP 变化并动态创建/回收内核 `tc filter` 规则，客户端下线或更换 IP 时自动平滑收敛。
  4. **全端交互与独立 Tab**：在入站配置弹窗中提供独立的「限速」Tab 选项卡，支持整入站与单客户端下行限速配置（Mbps），并在入站列表直观展示限速 Badge 状态，全量支持 13 种内置语言。
  5. **主从节点限速同步**：在多节点分布式集群部署下，主控节点修改的入站限速信息在增量对齐与心跳同步时（`wireInbound`）无缝同步给从节点，从节点接收配置后在本地实时通过 Linux TC 生效。
  6. **环境自适应与优雅降级**：支持自动探测默认路由网络接口或通过环境变量指定，非 Linux 或无网络流控权限环境下优雅静默跳过，不阻塞面板正常启动与业务运行。

## 客户端独立/差异化限速与入站流量计费倍率
- **价值与场景**：运营场景中，不同客户端用户购买的带宽等级不同，且同一用户访问优质专线入站与普通直连入站时需要不同的流控与计费策略；管理员需要为用户配置默认限速并在特定入站上覆盖限速，同时需要对高成本入站设置流量扣费倍率（如 1.5 倍）。
- **功能描述**：
  1. **客户端层级限速体系**：在客户端配置中增加全局默认下行限速（`downLimit`，Mbps），并在客户端编辑弹窗提供专属「限速」Tab。支持在关联入站列表中，为特定入站配置差异化限速覆盖（`downLimitByInbound`），未覆盖项自动继承全局限速。
  2. **客户端列表限速直观呈现**：在用户列表（`ClientsPage`）桌面端表格与移动端卡片中，展示限速 Badge 状态（如 `⚡ 100 Mbps (3 覆盖)`），悬浮查看各入站明细。
  3. **入站流量计费倍率（Traffic Multiplier）**：入站配置中新增流量倍率（`trafficRatio`，浮点数，默认 1.0）。在后台统计计算并扣除客户端已用流量配额时，按入站倍率加权计费（如倍率 0.5 扣除半价流量，倍率 2.0 扣除双倍流量）。
  4. **多节点客户端差异化限速与配额对齐**：主控节点向从节点下发客户端同步时，自动将主控入站映射至从节点本地入站 ID 作用域，隔离单入站限速，避免污染从节点全局限速，支持从节点独立展示与生效。

## 客户端单节点独立流量上限与用量监控
- **价值与场景**：在多节点混合运营场景中，不同节点成本差异巨大（如优质家宽/专线节点 vs 普通公网直连节点）。管理员需要为客户分配总流量套餐的同时，对高成本节点设置单独的使用上限，防止用户将总流量全部消耗在昂贵节点上；并在单节点用尽时精准熔断单节点，不影响其他节点正常使用。
- **功能描述**：
  1. **单节点独立流量上限与全端交互**：支持在客户端编辑弹窗的「流量」选项卡中，针对每个关联节点单独配置流量配额（如总配额 200GB，优质节点单独限额 50GB），表格直观呈现各入站已用量、限额、剩余及状态徽标。
  2. **精准单节点熔断与断流**：在客户端失效扫描中识别达到单节点限流上限的客户端，服务端 Xray 运行时仅将该客户端从该超额节点中注销断流，不修改客户端全局启用状态，客户端在其他节点连接与剩余配额依然完全正常。
  3. **双向记账与全自动流量自愈**：在本地入站统计及多节点同步场景下支持单节点流量双向原子累加记账；当客户端触发定时周期重置或管理员手动重置流量时，各节点已用量同步清零并自动恢复连接。
  4. **订阅展示页进度条呈现**：订阅链接配置中保留受限节点，并在订阅信息展示网页端（`/subscribe/:subId`）以「节点流量配额」卡片直观展现各限流节点的已用流量、总配额与动态进度条，节点熔断时展示醒目标签与高亮提示。



